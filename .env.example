# Used by drizzle-kit only (generate, migrate, push, studio).
# The Worker reaches Postgres through the Hyperdrive binding declared in
# apps/web/wrangler.jsonc, not through this variable.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/halo"
